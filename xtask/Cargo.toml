[package]
name = "xtask"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
rastreo-core = { path = "../rastreo-core", features = ["kafka", "http", "snmp", "arp", "ndp", "oui", "nats", "ssh", "icmp", "tls"] }
schemars = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
